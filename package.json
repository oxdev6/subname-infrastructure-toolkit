{
  "name": "ens-subname-infrastructure-toolkit",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "contracts",
    "backend",
    "dashboard",
    "sdk"
  ],
  "scripts": {
    "test:contracts": "npm --workspace contracts test",
    "dev:backend": "npm --workspace backend run dev",
    "test:backend": "npm --workspace backend test"
  }
}
